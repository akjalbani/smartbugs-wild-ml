# Core scientific stack
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
scipy>=1.10

# Deep-learning models (CNN, GNN). CPU wheels are fine; a GPU just makes it faster.
torch>=2.0

# Notebook niceties (already present on Colab; listed for local installs)
matplotlib>=3.7
tqdm>=4.65
